// eslint-disable-next-line no-restricted-imports
import {
  alpha,
  type PaletteColor,
  styled,
  type SxProps,
  type Theme,
  useTheme,
} from '@mui/material/styles'

export default styled

export {useTheme, SxProps, type Theme, PaletteColor, alpha}
